
# SPDX-License-Identifier: Apache-2.0
# Copyright 2019 Eotvos Lorand University, Budapest, Hungary

# Autogenerated file (from src/hardware_indep/multi/controlplane.stage.c.py:), do not modify directly.
# Generator: T4P4S (https://github.com/P4ELTE/t4p4s/)

import compiler_common
import re

def add_code(line, indent_level = 0, lineno = None, file = "src/hardware_indep/multi/controlplane.stage.c.py:"):
    global generated_code

    line_ends = {
        "line_comment": "\n",
        "inline_comment": "",
        "no_comment": "\n",
        "no_comment_inline": "",
    }

    sugar_style = compiler_common.file_sugar_style[-1]

    stripped_line = line.strip()
    no_sugar_on_line = stripped_line.startswith('//') or stripped_line.startswith('# ') or stripped_line == ""

    indent = compiler_common.file_indent_str * indent_level
    if 'inline' in compiler_common.file_sugar_style[-1]:
        indent = ''

    sugared = sugar(no_sugar_on_line, file, lineno, sugar_style)
    line_end = line_ends[sugar_style]
    return f'{indent}{stripped_line}{sugared}{line_end}'


def sugar(no_sugar_on_line, file, lineno, sugar_style):
    if True or no_sugar_on_line or file is None or lineno is None:
        return ""

    if sugar_style == 'line_comment':
        return f" // {file}{lineno}"
    if sugar_style == 'inline_comment':
        return f" /* {file}{lineno} */"
    return ""


generated_code += "// Autogenerated file (from src/hardware_indep/multi/controlplane.stage.c.py: via hardware_indep/multi/controlplane.stage.c.py), do not modify directly.\n"
generated_code += "// Generator: T4P4S (https://github.com/P4ELTE/t4p4s/)\n"
generated_code += "\n"

# SPDX-License-Identifier: Apache-2.0 ## src/hardware_indep/multi/controlplane.stage.c.py:2
# Copyright 2016 Eotvos Lorand University, Budapest, Hungary ## src/hardware_indep/multi/controlplane.stage.c.py:3

generated_code += add_code("")
compiler_common.current_compilation['is_multicompiled'] = True ## src/hardware_indep/multi/controlplane.stage.c.py:5

generated_code += add_code("")
part_count = compiler_common.current_compilation['multi'] ## src/hardware_indep/multi/controlplane.stage.c.py:7
multi_idx = compiler_common.current_compilation['multi_idx'] ## src/hardware_indep/multi/controlplane.stage.c.py:8

generated_code += add_code("")
all_tables = sorted(hlir.tables, key=lambda table: len(table.actions)) ## src/hardware_indep/multi/controlplane.stage.c.py:10
tables = list(table for idx, table in enumerate(all_tables) if idx % part_count == multi_idx) ## src/hardware_indep/multi/controlplane.stage.c.py:11

generated_code += add_code("")
if tables == []: ## src/hardware_indep/multi/controlplane.stage.c.py:13
    compiler_common.current_compilation['skip_output'] = True ## src/hardware_indep/multi/controlplane.stage.c.py:14
else: ## src/hardware_indep/multi/controlplane.stage.c.py:15
    generated_code += add_code(' #define T4P4S_MULTI_IDX {}'.format(multi_idx), lineno = 15)
    generated_code += add_code(" #include \"multi_controlplane.c\"", lineno = 16)
